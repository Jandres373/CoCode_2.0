import { Poppins } from 'next/font/google'

export const THIN = Poppins({ subsets: ['latin'], weight: '400' })
export const REGULAR = Poppins({ subsets: ['latin'], weight: '500' })
export const SUBTITLE = Poppins({ subsets: ['latin'], weight: '600' })
export const TITLE = Poppins({ subsets: ['latin'], weight: '700' })
